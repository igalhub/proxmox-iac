---
# Real k3s server install (--disable=traefik, capture join token) lands in
# PX-008 — see docs/SPEC.md §7 build order. This is a structural stub only.
- name: K3s-server role placeholder
  ansible.builtin.debug:
    msg: "k3s-server install/join implemented in PX-008"
