configKubernetes:
  # wk-1 hosts Postgres per docs/SPEC.md's role split (§1).
  node_readiness_label: {}

configPostgresPodResources:
  default_cpu_request: 100m
  default_cpu_limit: "1"
  default_memory_request: 128Mi
  default_memory_limit: 512Mi

resources:
  requests:
    cpu: 50m
    memory: 100Mi
  limits:
    memory: 250Mi
